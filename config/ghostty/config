# Ghostty 配置（由 home-manager 托管）
# 修改后运行: home-manager switch --flake ~/nix-config
#
# 注：ghostty 本体由 pacman 管理（extra/ghostty）。
# 2026-08-07 曾尝试迁移到 Nix，但 Nix 版 glibc(2.42) 与系统 GL 栈
# 要求的 glibc(2.43+) 不兼容，EGL 渲染失败，故改回 pacman。

# 常用配置示例（按需取消注释）：
# theme = one-dark
# font-family = JetBrains Mono
# font-size = 12
# background-opacity = 0.9
# window-padding-x = 8
# window-padding-y = 8
