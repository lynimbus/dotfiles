# 全局指令

## 声明式环境

本机环境由 NixOS flake 声明式管理，配置仓在 `~/dotfiles`（该目录下有项目级 AGENTS.md，进入时自动加载）。

## 上下文与安全

- 大文件先 `rg -n` 定位再分段读；批量统计/替换用脚本聚合，不要一行行读。
- 长会话把决策、待办、验证结果写进项目 `CONTEXT.md`，compact 后可恢复。
- 禁止把独立二进制放进 `~/.pi/agent/bin` 遮蔽系统 PATH。
- 回答简洁：先结论，给完整文件路径和验证结果。没验证就说没验证。
